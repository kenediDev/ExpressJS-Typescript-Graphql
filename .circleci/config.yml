version: 2.1

orbs:
  node: circleci/node@14.16.0

jobs:
  test:
    executor: node/default
    steps:
      - checkout
      - node/install-packages:
        override-ci-command: npm install
      - run: npm run test

workflows:
  test_my_app:
    - test
